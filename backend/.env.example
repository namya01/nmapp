# Backend Environment Configuration
PORT=5000
NODE_ENV=development

# JWT Secret for access token signing
JWT_SECRET=student-committee-platform-jwt-secret-key-2026

# PostgreSQL Connection (optional; falls back to embedded persistent relational store)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/student_committee_db

# AWS S3 & Textract (optional for cloud production)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=student-committee-assets
